{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    <div id="{{ particle.id }}" class="g-promo" style="background-image: url({{ url(particle.image)|e }});">
        {% if particle.text %}{{ particle.text|raw }}{% endif %}
    </div>
{% endblock %}

