name: Main Styles
description: Main content styles for the theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "rgba(255, 255, 255, 0)"
    text-color:
      type: input.colorpicker
      label: Text
      default: "#030303"
